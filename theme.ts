// src/theme/theme.ts

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#ffffff', // White color for primary elements like buttons
        },
        text: {
            primary: '#ffffff', // Set text color to white
        },
    },
});

export default theme;
